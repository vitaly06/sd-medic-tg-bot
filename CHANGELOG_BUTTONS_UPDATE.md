# Обновление: Замена команд на кнопки

## Дата: 20 декабря 2025

## Изменения

### 1. Исправление выбора количества товара в корзине ✅

**Проблема:** При добавлении товара в корзину всегда добавлялась только 1 штука.

**Решение:**

- Заменили текстовую команду `/add123 2` на **inline-кнопки** с выбором количества
- При просмотре товара в каталоге появляются кнопки:
  - `1 шт`, `2 шт`, `3 шт`, `5 шт`, `10 шт`
  - `✏️ Указать количество` - для произвольного количества
- Количество корректно передается в корзину

### 2. Добавление кнопки "Пропустить" при заполнении анкеты ✅

**Проблема:** При заполнении анкеты нужно было вручную писать "пропустить".

**Решение:**

- Добавлена кнопка `⏩ Пропустить` на всех шагах заполнения анкеты:
  - Дата МСЭ
  - Дата первого получения ТСР
  - Способ получения ТСР
  - Виды ТСР
  - Дата следующего получения ТСР
- Создана функция `isSkipCommand()` для универсальной проверки команды пропуска

### 3. Замена команд на кнопки ✅

#### Управление корзиной:

**Было:**

- `/add123 2` - добавить товар
- `/set 1 3` - изменить количество
- `/remove 1` - удалить товар

**Стало:**

- Inline-кнопки при просмотре товара для добавления с выбором количества
- В корзине появляются кнопки для каждого товара:
  - `Изменить кол-во` - открывает диалог для ввода нового количества
  - `Удалить` - моментально удаляет товар

#### Управление обращениями поддержки:

**Было:**

- `/reply 123 текст ответа` - ответить на обращение
- `/close 123` - закрыть обращение

**Стало:**

- При просмотре обращения появляются inline-кнопки:
  - `💬 Ответить` - открывает диалог для ввода ответа
  - `✅ Закрыть обращение` - закрывает тикет с уведомлением пользователя

## Технические детали

### Новые обработчики состояний:

- `add_custom_quantity` - ввод произвольного количества товара
- `cart_change_quantity` - изменение количества товара в корзине
- `support_reply_message` - ответ на обращение от поддержки

### Новый callback_query handler:

- Обработка inline-кнопок для товаров (`add_`, `cart_change_`, `cart_remove_`)
- Обработка inline-кнопок для поддержки (`reply_ticket_`, `close_ticket_`)

### Улучшенная функция `isSkipCommand()`:

- Поддерживает как текстовый ввод "пропустить", так и кнопку "⏩ Пропустить"
- Используется во всех местах проверки команды пропуска

## Преимущества:

1. ✅ **Удобство**: Не нужно вводить команды вручную
2. ✅ **Меньше ошибок**: Кнопки исключают опечатки в командах
3. ✅ **Интуитивность**: Пользователь видит доступные действия
4. ✅ **Скорость**: Быстрее нажать кнопку, чем писать команду
5. ✅ **Современность**: Соответствует стандартам UX современных ботов

## Совместимость:

- ✅ Старые команды `/add`, `/set`, `/remove`, `/reply`, `/close` по-прежнему работают
- ✅ Можно писать "пропустить" вручную или использовать кнопку
- ✅ Все изменения обратно совместимы

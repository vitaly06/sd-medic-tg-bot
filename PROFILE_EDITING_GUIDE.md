# Руководство по редактированию профилей пользователей

## Обзор функциональности

Система редактирования профилей позволяет как пользователям редактировать свои данные, так и администраторам редактировать профили любых пользователей.

## Возможности пользователя

### Редактирование своего профиля

Пользователь может редактировать свой профиль через:

1. **Кнопку "👤 Профиль"** в главном меню
2. **Кнопку "✏️ Редактировать профиль"** в меню профиля

### Доступные поля для редактирования:

- **📅 Дата МСЭ** - Дата заключения МСЭ по ИПРа
- **📅 Дата первого ТСР** - Дата первого получения ТСР
- **📅 След. получение ТСР** - Дата следующего получения ТСР (можно указать вручную)
- **📋 Способ получения** - Выдача или сертификат
- **📋 Виды ТСР** - Список назначенных ТСР (через запятую)
- **⏱ Периодичность ТСР** - Как часто получать ТСР (в месяцах: 3, 6, 12)
- **⏰ Срок напоминания** - За сколько дней напоминать о получении (21, 14, 7)

### Особенности:

- При изменении периодичности ТСР дата следующего получения **пересчитывается автоматически**
- Любое поле можно очистить, написав **"пропустить"**
- **Уведомления ТСР** можно включить/выключить отдельной кнопкой

## Возможности администратора

### Редактирование профиля пользователя

Администратор может редактировать профиль любого пользователя:

1. **Найти пользователя:**
   - Админ панель → "🔍 Поиск пользователей"
   - Ввести имя, username или ID

2. **Выбрать пользователя** из списка результатов (по номеру)

3. **Нажать "📋 Редактировать профиль"** в меню действий с пользователем

4. **Выбрать поле** для редактирования (те же поля, что у пользователя)

5. **Ввести новое значение** или "пропустить" для очистки

### Меню действий с пользователем:

```
Пользователь: Иван Иванов @ivan
ID: 5 | TG ID: 123456789
Роль: user
Статус: ✅ Активен

Выберите действие:
┌──────────────────────────────┐
│ 🚫 Заблокировать            │
│ 📋 Редактировать профиль    │
│ 👤 Изменить роль            │
│ 🗑 Удалить пользователя     │
│ ◀️ Назад                    │
└──────────────────────────────┘
```

### Связь профиля с пользователем

- Профиль автоматически связан с пользователем через `userId`
- При поиске пользователя по нику/имени, профиль будет найден автоматически
- Админ видит полную информацию профиля перед редактированием

## Технические детали

### Структура данных (UserProfile)

```typescript
{
  userId: number;              // Связь с пользователем
  mseDate?: Date;              // Дата МСЭ
  firstTsrDate?: Date;         // Дата первого получения ТСР
  nextTsrDate?: Date;          // Дата следующего получения
  tsrMethod?: string;          // Способ получения
  tsrTypes?: string;           // Виды ТСР
  tsrPeriodMonths: number;     // Периодичность (по умолчанию 3)
  reminderDaysBefore: number;  // За сколько дней напоминать (по умолчанию 21)
  notificationsEnabled: boolean; // Включены ли уведомления
  lastReminderSent?: Date;     // Последнее напоминание
}
```

### Автоматический расчет дат

При изменении **периодичности ТСР** или **даты первого получения**, система автоматически пересчитывает **дату следующего получения** по формуле:

```
nextTsrDate = firstTsrDate + (tsrPeriodMonths * N месяцев)
```

где N - количество периодов до ближайшей будущей даты.

### Состояния (States)

**Для пользователя:**

- `edit_profile_mse_date`
- `edit_profile_first_tsr_date`
- `edit_profile_next_tsr_date`
- `edit_profile_tsr_method`
- `edit_profile_tsr_types`
- `edit_profile_tsr_period`
- `edit_profile_reminder_days`

**Для администратора:**

- Используются те же состояния, но с дополнительным полем `data.targetUser`
- При сохранении изменений админ возвращается в админ-панель

### Форматы ввода данных

| Поле             | Формат              | Примеры                                |
| ---------------- | ------------------- | -------------------------------------- |
| Дата             | ДД.ММ.ГГГГ          | 15.01.2024                             |
| Периодичность    | Число (1-24)        | 3, 6, 12                               |
| Срок напоминания | Число (1-60)        | 7, 14, 21                              |
| Способ получения | Текст               | выдача, сертификат                     |
| Виды ТСР         | Текст через запятую | коляска, протезы, ортопедическая обувь |

### Валидация

- **Даты:** Проверка формата ДД.ММ.ГГГГ
- **Периодичность:** 1-24 месяца
- **Срок напоминания:** 1-60 дней
- **Ошибки:** При неверном формате пользователь получает сообщение с инструкцией

## Примеры использования

### Пример 1: Пользователь редактирует свой профиль

1. Нажимает "👤 Профиль"
2. Видит текущие данные
3. Нажимает "✏️ Редактировать профиль"
4. Выбирает "📅 Дата первого ТСР"
5. Вводит: `15.03.2024`
6. Получает подтверждение: "✅ Дата первого получения ТСР обновлена!"

### Пример 2: Админ редактирует профиль пользователя

1. Открывает админ панель
2. Нажимает "🔍 Поиск пользователей"
3. Вводит: `@ivan_petrov`
4. Выбирает пользователя: `1`
5. Нажимает "📋 Редактировать профиль"
6. Видит профиль пользователя
7. Выбирает "⏱ Периодичность ТСР"
8. Вводит: `6`
9. Получает подтверждение с автопересчетом даты

### Пример 3: Очистка поля

1. Выбирает поле для редактирования
2. Вводит: `пропустить`
3. Поле очищается (устанавливается в `null` или `undefined`)

## Интеграция с системой напоминаний

Данные профиля используются в **системе автоматических напоминаний о получении ТСР**:

- **Периодичность** определяет, как часто отправлять напоминания
- **Срок напоминания** определяет, за сколько дней до даты напомнить
- **Уведомления** позволяют отключить напоминания для конкретного пользователя

Подробнее см. [TSR_REMINDERS_SYSTEM.md](./TSR_REMINDERS_SYSTEM.md)

## Безопасность

- Обычные пользователи могут редактировать **только свой профиль**
- Администраторы могут редактировать **любой профиль**
- Все изменения применяются **немедленно** в базе данных
- История изменений не ведется (можно добавить при необходимости)

## Возможные улучшения

1. **История изменений профиля** - логирование всех изменений
2. **Массовое редактирование** - изменение профилей нескольких пользователей
3. **Импорт/экспорт профилей** - загрузка данных из Excel
4. **Валидация дат** - проверка логичности дат (например, nextTsrDate > firstTsrDate)
5. **Уведомления администратору** - оповещение при изменении критичных полей
